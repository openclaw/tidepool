{
  "name": "tidepool",
  "private": true,
  "description": "Experimental OpenClaw plugins: themes, critters, toys, and other small creatures.",
  "packageManager": "pnpm@12.4.0",
  "scripts": {
    "build": "pnpm -r run build",
    "check": "pnpm -r run check"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "openclaw": "^2026.9.5",
    "typescript": "^6.0.0"
  }
}
